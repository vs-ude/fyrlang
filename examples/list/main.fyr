import {
	"collections/list"
}

func Main() {
	// l := new list.List<int>
	var l mut *list.List<string> = {}
	l.Push("42")
	l.Push("84")
	// for e := l.First(); e != null; e = e.Next() {
		// println(e.Value)
	// }
	println("Ok")
}
