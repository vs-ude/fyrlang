type S struct {
    X int
    Y int
    Next *S
    Iso -> *S
}

func dome(ptrA -a mut *S, ptrB -b mut *S) {
    var ptr1 mut *S = {}
    ptr1.Next = ptrA
    ptr1.Next = ptrB
}

func domore(ptrA -a mut *S, ptrB -b mut *S) {
    var ptr mut *S = {}
    ptr.Next = ptrA
    for i := 0; i < 10; i++ {
        ptr = ptrB
    }
    ptr.Next = ptrA
}

func Main() {

}