language: go
dist: bionic

go:
  - stable
  - "1.11"
  - "1.10"

env:
  - TEST_SUITE=go
  - TEST_SUITE=fyr

before_install:
  - if [ $TEST_SUITE = fyr ]; then sudo apt update && sudo apt install valgrind; fi

script:
  - make
  - SLOW_TESTS=y make test_${TEST_SUITE}
